{
  "name": "urban-clothing-pos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Retail clothing store point-of-sale architectural prototype",
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test",
    "test:integration": "node --test test/api.integration.test.js",
    "check": "node --check js/core.js && node --check js/api.js && node --check js/app.js && node --check server/api.js && node --check server/database.js && node --check server/pos-service.js && node --check tools/serve.mjs",
    "db:setup": "node tools/database.mjs setup",
    "db:test": "node tools/database.mjs test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
